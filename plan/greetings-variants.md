# ✅ PLAN COMPLETE
# Greeting Variants

## Status: COMPLETED

## Scope
- Update `json/config/greetings.json` only.
- Replace the repeated greeting text in the four existing entries with four distinct variants.
- Keep the `|` prefix at the start of every line and preserve the final prompt line exactly, allowing color codes.

## Risks / Notes
- The file uses custom JSON formatting, so editor linting may complain even when the in-game parser accepts the text.
- No schema changes are required; only the string bodies will change.

## Status: COMPLETED

2026-04-11
