version: "3.8"
services:
  app:
    image: basemud:latest
    build:
      context: .
    volumes:
      - ./log:/opt/rom/log:rw
      - ./player:/opt/rom/player:rw
      - ./json/area:/opt/rom/json/area:rw
    ports:
      - "4000:4000"
